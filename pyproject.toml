[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "desktop-agent"
version = "0.1.0"
description = "A desktop automation agent"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    # Add dependencies from requirements.txt
]

[tool.setuptools.packages.find]
where = ["."]
include = ["desktop_agent*"]